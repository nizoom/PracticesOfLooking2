import React, { useEffect } from "react";
import "./sketcher.css";
import { Canvas, useFrame } from "@react-three/fiber";
// a scene is, a camera, mesh, geometry, material, fork
const Sketcher = () => {
  useEffect(() => {});
  return <div className="sketcher-wrapper"></div>;
};

export default Sketcher;
